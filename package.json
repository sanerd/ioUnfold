{
  "name": "iounfold-monorepo",
  "version": "1.0.0",
  "private": true,
  "packageManager": "pnpm@10.34.4",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "test": "turbo test",
    "clean": "turbo clean && rm -rf node_modules"
  },
  "devDependencies": {
    "@types/node": "^26.1.0",
    "turbo": "^2.3.0",
    "typescript": "^5.5.0"
  }
}
